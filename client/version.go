package rig

// RigdVersion is the rigd server version this SDK targets.
// Tagged separately as rigd/v{RigdVersion} in git.
// Bump when rigd changes. Client-only changes don't require a bump.
const RigdVersion = "0.2.0"
